import 'package:flutter/material.dart';

class DictionaryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Здесь будет словарь жестов',
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
